import { addCollection } from '@iconify/react';
import { icons as lucideIcons } from '@iconify-json/lucide';

// Register Lucide locally so lucide:* icons render without waiting for Iconify API.
addCollection(lucideIcons);
